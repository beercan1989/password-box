<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/passwordScrollView"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/LinearLayout01"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:orientation="vertical" >
        
        <TableLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent" >

            <TableRow>
                <TextView android:text="Before Value:"/>
                <TextView android:id="@+id/encryptedPassword"/>
            </TableRow>
            <TableRow>
                <Button android:id="@+id/decryptButton" android:text="Decrypt"/>
            </TableRow>
            <TableRow>
                <TextView android:text="After Value:"/>
                <TextView android:id="@+id/decryptedPassword"/>
            </TableRow>
        </TableLayout>
        
        <Button
            android:id="@+id/Button01"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:text="Flip" >
        </Button>

    </LinearLayout>

</ScrollView>